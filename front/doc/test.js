describe('Julietest', () => {

	it('Should RunApp', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="loginUsername"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka@gmail.com')
		cy.get('[data-test="loginPassword"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('julineLingo')
		cy.get('[data-test="loginSend"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="newBookTitle"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('The Secret (EN)')
		cy.get('[data-test="newBookCreate"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="newSentence"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="newPart"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="partOriginal"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('The')
		cy.get('[data-test="partTranslation"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('To')
		cy.get('[data-test="partSave"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="newPart"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="partOriginal"]:eq(' + ('1' ? '1' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('Secret')
		cy.get('[data-test="partTranslation"]:eq(' + ('1' ? '1' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('Tajemství')
		cy.get('[data-test="partSave"]:eq(' + ('1' ? '1' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="sentenceTranslation"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('Tajemství (název knihy)')
		cy.get('[data-test="sentenceNote"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('Toto se většinou nepřekládá. Je to "The Secret" nebo "Secret"')
		cy.get('[data-test="sentenceSave"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="partDelete"]:eq(' + ('1' ? '1' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="partDeleteConfirm"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="sentenceDelete"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="sentenceDeleteConfirm"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="bookDelete"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="bookDeleteConfirm"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})
	it('Should ViewBook', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="loginUsername"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka@gmail.com')
		cy.get('[data-test="loginPassword"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('julineLingo')
		cy.get('[data-test="loginSend"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="bookListItem"]:eq(' + ('0' ? '0' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})

})